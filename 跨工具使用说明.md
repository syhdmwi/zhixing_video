# 跨工具使用说明

这套包最初按 Codex skills 的目录结构整理，但核心内容不是 Codex 专用。

它本质上是一套 AI 短视频生产工作流规则，可以迁移到：

- Codex
- OpenClaw
- Trae
- Claude Code
- 其它支持项目规则、上下文文档或自定义工作流的 AI 编程工具

## 核心思路

不同工具的“技能系统”名字可能不同。

有的叫 skill，有的叫 rules，有的叫 memory，有的叫 project instructions，有的只是把 Markdown 文件放进项目上下文。

只要工具能读取这些说明，并按说明执行，就可以使用这套工作流。

## 通用唤醒规则

建议在 Codex、OpenClaw、Trae、Claude Code 等工具里都加入这条规则：

```text
当用户提到“知行视频skill”“知行视频 skill”“知行视频工作流”，或说“使用知行视频skill帮我制作视频/图片”时，优先读取根目录 SKILL.md，并按其中流程引导用户。用户说“做数字人”“用我的声音读”或“语音克隆”时，回复“该功能当前暂未开放”。
```

如果用户只说了唤醒词，没有给文案、图片、音频或人物视频，应先问：

```text
你想用知行视频skill做哪一步？
1. 从文案开始做图片
2. 从图片继续做视频
3. 从文案一路跑完整流程
```

## 最重要的文件

如果你的工具不能直接识别整个 skill 目录，优先导入这些文件：

1. [SKILL.md](./SKILL.md)
2. [用户使用说明](./用户使用说明.md)
3. [3分钟快速开始](./3分钟快速开始.md)
4. [ai-short-video-pipeline/MODULE.md](./ai-short-video-pipeline/MODULE.md)
5. [ai-short-video-pipeline/references/user-guidance-templates.md](./ai-short-video-pipeline/references/user-guidance-templates.md)
6. [ai-short-video-pipeline/references/workflow-state-machine.md](./ai-short-video-pipeline/references/workflow-state-machine.md)
7. [ai-short-video-pipeline/references/project-state-template.json](./ai-short-video-pipeline/references/project-state-template.json)
8. [ai-video-image-prompts/MODULE.md](./ai-video-image-prompts/MODULE.md)
9. [ai-video-prompt-to-images/MODULE.md](./ai-video-prompt-to-images/MODULE.md)
10. [ai-video-generate-videos/MODULE.md](./ai-video-generate-videos/MODULE.md)

其中对外总控入口是：

- [SKILL.md](./SKILL.md)

## 防止流程走错

Trae、OpenClaw、Claude Code 这类工具有时会把文档当作普通上下文，而不是像 Codex 一样自动记住 skill 状态。所以用户从中间环节输入 `1`、`2`、`全部生图`、`进入视频生成` 时，工具可能不知道这句话属于哪一步。

解决办法是把状态机规则加入项目上下文，并要求工具每次执行前先恢复当前阶段：

```text
当用户从任意环节进入，或只输入 1、2、全部生图、直接生成视频等短指令时，必须先读取 workflow-state-machine.md，确认 current_stage 和前置条件。做数字人、用我的声音读、语音克隆等短指令固定挡回“该功能当前暂未开放”。
如果状态不明确，先用“当前流程状态”模板恢复流程，再继续下一步。
不要在前置条件缺失时直接生图、生成视频、保存模板或导出剪映交付包。
```

推荐恢复流程话术：

```text
使用知行视频skill，帮我恢复当前流程状态。
```

## 在 Codex 里使用

把整个项目目录放进 Codex 可读取的位置，并以根目录 [SKILL.md](./SKILL.md) 作为主入口。
如果你仍然沿用技能目录方式，也应让 Codex 优先读取根入口，而不是让用户自己挑内部模块。

使用时直接说：

```text
这是我的文案，帮我从头开始做。
```

### 正式生图执行补充

无论你是在 `Codex`、`Trae`、`OpenClaw` 还是 `Claude Code` 里使用这套 skill,正式生图阶段都统一走外部脚本 / API。

执行路径固定为：

- `任意工具 + GPT-Image-2`
  - 统一走一加 Image-2 外部脚本 / API

`nanobanana-2` 已停用，不再作为生图选项。

这样做的原因是：

- 更容易保持一镜一任务
- 更不容易串图
- 更不容易把多个分镜误合成一张
- 更适合队列执行、保留任务日志和轮询状态

## 在 OpenClaw 里使用

推荐方式：

1. 把整个项目文件夹加入 OpenClaw 工作区
2. 把 [SKILL.md](./SKILL.md) 设置为总控规则或主上下文
3. 把 [用户使用说明](./用户使用说明.md) 发给使用者作为入口文档
4. 让 OpenClaw 按目录里的模块说明调用对应脚本或流程

建议给 OpenClaw 的开场指令：

```text
请读取这个项目根目录中的 SKILL.md，把它作为 AI 短视频工作流总控规则。
同时读取 ai-short-video-pipeline/references/workflow-state-machine.md 和 project-state-template.json，用它们判断当前流程阶段。
当用户提到“知行视频skill”时，优先唤醒这个工作流。
用户发文案后，按用户使用说明里的流程一步一步引导。
```

## 在 Trae 里使用

推荐方式：

1. 用 Trae 打开整个项目文件夹
2. 把 [SKILL.md](./SKILL.md) 和 [用户使用说明](./用户使用说明.md) 加入项目上下文
3. 把 [TRAE_PROJECT_RULES.md](./TRAE_PROJECT_RULES.md) 里的规则完整粘贴到 Trae 的项目规则
4. 让 Trae 优先按总控规则判断该调用哪个子模块

如果不加 Trae 项目规则，只把长文档丢给它，它很容易出现这几类问题：

- 自动省略确认步骤
- 把 `1` / `2` 提前解释错
- 用户说“继续”时直接跳到后面阶段
- 把文档当建议，而不是当 gate

所以在 Trae 里，`TRAE_PROJECT_RULES.md` 不是可选项，建议当必装项。

建议给 Trae 的开场指令：

```text
请把当前项目当作 AI 短视频工作流包使用。
优先阅读 用户使用说明.md、SKILL.md、workflow-state-machine.md 和 project-state-template.json。
同时严格遵守 TRAE_PROJECT_RULES.md，不允许跳步骤、省略确认或跨阶段解释 1/2。
当用户提到“知行视频skill”时，优先进入这个工作流。
当我输入文案时，请按流程引导我完成图片、视频或剪映交付包。
当我从中间步骤输入 1、2、全部生图、进入视频生成等短指令时，先判断 current_stage 和前置条件，不要跳错流程。
```

## 在 Claude Code 里使用

推荐方式：

1. 用 Claude Code 打开整个项目目录
2. 把 [用户使用说明](./用户使用说明.md) 当作使用者入口
3. 把 [SKILL.md](./SKILL.md) 当作主流程规则
4. 需要图片、视频或剪映交付包时，再读取对应内部模块的 `MODULE.md`

建议给 Claude Code 的开场指令：

```text
请读取当前项目里的 用户使用说明.md 和 SKILL.md。
同时读取 ai-short-video-pipeline/references/workflow-state-machine.md 和 project-state-template.json。
之后把这个项目当作 AI 短视频生产工作流包使用。
当用户提到“知行视频skill”时，优先进入这个工作流。
不要先解释内部模块名，先按用户任务一步一步引导。
每次执行前先确认当前流程阶段和前置条件。
```

## 通用启动话术

无论你用哪个工具，安装或导入完成后，都可以这样开始：

```text
使用知行视频skill，帮我从文案开始做视频。
```

或者：

```text
这是我的文案，帮我从头开始做。
```

或者：

```text
我想测试这套短视频流程，你一步一步带我走。

文案是：
[把你的文案粘贴在这里]
```

## 如果工具不支持直接执行脚本

有些工具只能读文档，不能直接运行脚本。

这种情况下也能使用：

- 让工具先生成图片提示词
- 再把提示词复制到你自己的生图工具
- 再把生成好的图片交回工具继续做视频提示词或剪辑规划

也就是说，这套包可以分成两种使用方式：

- 自动执行版：工具能运行脚本和 API
- 流程指导版：工具只负责生成提示词、清单、操作步骤

## 最小可用导入

如果你只想快速试用，不想导入全部文件，最少导入这 3 个：

1. [用户使用说明](./用户使用说明.md)
2. [SKILL.md](./SKILL.md)
3. [ai-short-video-pipeline/references/workflow-state-machine.md](./ai-short-video-pipeline/references/workflow-state-machine.md)
4. [ai-video-image-prompts/MODULE.md](./ai-video-image-prompts/MODULE.md)

这样至少可以跑通：

- 文案理解
- 风格选择
- 主体识别
- 图片提示词生成

等确认流程有用后，再补充生图、视频和剪映交付模块。
